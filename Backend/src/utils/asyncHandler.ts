// import { Request, Response, NextFunction } from "express";

// const asyncHandler = (fun) => {
//   return (Promise.resolve(fun(req: Request, res: Response, next: NextFunction)).catch((error) => next(error)));
// };
